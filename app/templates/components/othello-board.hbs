{{#each-in game.board as |rowKey rowValues|}}
  {{#each-in rowValues as |columnKey columnValue|}}
    {{board-cell contents=columnValue rowIndex=rowKey columnIndex=columnKey click=(action "playTurn" rowKey columnKey)}}
  {{/each-in}}
{{/each-in}}
